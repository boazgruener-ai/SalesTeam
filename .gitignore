# Scraped lead data, full settings/lead backups, and activity-log exports —
# contain real people's names, job titles, employers, and LinkedIn profile
# URLs scraped from their public posts. Never publish this; code and docs
# only. Ignored by folder rather than filename pattern - a filename-pattern
# approach silently stopped covering salesteam-auto-leads-backup-*/
# salesteam-auto-settings-backup-* when the combined backup file was split
# into two (v0.25.1), which a folder-level rule can't drift out of sync with.
/backup/
/exports/
/log/

# Unredacted screenshots that show real LinkedIn users' names/companies/emails
# (the "-redacted"/"-cleaned" versions of these are the ones meant for public use)
Screen shots/results.png
Screen shots/Sample message drafted by my Sales Mentor Agent.jpg
Screen shots/image-1788184816990.png
Screen shots/image-1788184825115.png

# Unrelated personal phone screenshot, not project content
Screen shots/Screenshot 2026-08-31 155920.jpg

# Pitch deck kept out of the public repo entirely (contact-slide email,
# course-assignment framing) - and its generator script, since it only
# exists to build that deck and has the same email hardcoded in it.
SalesTeam - Pitch Deck*.pptx
SalesTeam - Pitch Deck.pdf
build_deck.py
